spring:
  application:
    name: Y_LAB
  datasource:
    name: db
    password: 1234
    username: caxap
    url: jdbc:postgresql://localhost:5434
server:
  port: 8080
springdoc:
  swagger-ui:
    path: /swagger-ui
  default-produces-media-type: application/json